import loading_spinner from '../Disk-1s-217px.gif';

const Spinner = () => {
    return (
        <div>
            <img src={loading_spinner} alt="loading..." />
        </div>
    );
}

export default Spinner;
